<!-- <h1><%= ENV["app_site_title"] %></h1>
<p><%= link_to("Images", images_path) %></p>
<p><span id="toggle_fullscreen">Fullscreen</a></span>

<div class="slider" id="main-slider">
  <div class="slider-wrapper">
    <% @images.each do |image, index| %>
      <%= image_tag(image.data_url(:projector), class: 'slide') %>
    <% end %>
  </div>
</div> -->

<div id="myCarousel" class="carousel slide" data-ride="carousel">
  <ol class="carousel-indicators">
  <% @images.each_with_index do |image, index| %>
    <li data-target="#myCarousel" data-slide-to="<%= index %>"<% if index == 0 %> class="active"><% end %></li>
  <% end %>
  </ol>
  <div class="carousel-inner">
  <% @images.each_with_index do |image, index| %>
    <div class="carousel-item<% if index == 0 %> active<% end %>">
      <%= link_to(image_tag(image.data_url(:projector), class: 'second-slide'), image_path(image)) %>
      <div class="container">
        <div class="carousel-caption">
          <!-- <h1>Headline.</h1>
          <p>Example paragraph</p>
          <p><a class="btn btn-lg btn-primary" href="#" role="button">Button</a></p> -->
        </div>
      </div>
    </div>
  <% end %>
  </div>
  <a class="carousel-control-prev" href="#myCarousel" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#myCarousel" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>

<section class="jumbotron text-center">
  <div class="container">
    <h1 class="jumbotron-heading">Draw Your Pain</h1>
    <p class="lead text-muted">Visitors are invited to draw their pain. This is the moderation interface.</p>
    <!-- <p class="lead text-muted">It'll look like this:</p> -->
    <p>
      <%= link_to "See the #{number_with_delimiter(@image_count, delimiter: ',')} images", images_path, class: 'btn btn-outline-secondary my-2' %>
    </p>
  </div>
</section>